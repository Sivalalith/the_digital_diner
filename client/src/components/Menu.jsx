import React from "react";

// BE menu response json structure
/**
 * {
 *  "success": "true",
 *  "menu": {
 *    "starters": [{...}],
 *    "desserts": [{...}],
 *    "drinks": [{...}]...
 *   }
 * }
 */

const Menu = ({ searchQuery }) => {
  // allItems state var -> to store all the items in db
  // ---> fetchDataFromDb()
  // filteredItems state var -> to store the items filtered by search query
  return <div>Menu</div>;
};

export default Menu;
